import Example from '~/components/example'
import { Code, InlineCode, RequestHeader } from '~/components/text/code'
import Note from '~/components/text/note'
import { GenericLink, HelpLink } from '~/components/text/link'
import { P, PDIV } from '~/components/text/paragraph'
import Request from '~/components/api/request'
import EndpointComponent from './endpointComponent.jsx'

## Deployments

<EndpointComponent oldSpec={props.oldSpec} spec={props.spec} updateSpec={props.updateSpec} method="POST" url="/v6/now/deployments" />
<EndpointComponent oldSpec={props.oldSpec} spec={props.spec} updateSpec={props.updateSpec} method="POST" url="/v2/now/files" />
<EndpointComponent oldSpec={props.oldSpec} spec={props.spec} updateSpec={props.updateSpec} method="GET" url="/v3/now/deployments" />
<EndpointComponent oldSpec={props.oldSpec} spec={props.spec} updateSpec={props.updateSpec} method="DELETE" url="/v6/now/deployments/:id" />
<EndpointComponent
oldSpec={props.oldSpec}
  spec={props.spec} updateSpec={props.updateSpec}
  method="GET"
  url="/v5/now/deployments/:id/files"
/>
<EndpointComponent
oldSpec={props.oldSpec}
  spec={props.spec} updateSpec={props.updateSpec}
  method="GET"
  url="/v5/now/deployments/:id/files/:fileId"
/>
<EndpointComponent
oldSpec={props.oldSpec}
  spec={props.spec} updateSpec={props.updateSpec}
  method="GET"
  url="/v5/now/deployments/:id/builds"
/>
